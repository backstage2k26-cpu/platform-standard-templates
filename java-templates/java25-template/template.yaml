apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: java25-template
  title: Java 25 Template
  description: Create a Java 25 service repository in GitHub
  tags:
    - java
    - jenkins
    - sonarqube
spec:
  owner: user:default/guest
  type: service

  parameters:
    - title: Repository Details
      required:
        - repoOwner
        - repoName
        - owner
      properties:
        repoOwner:
          title: Repository Owner
          type: string
          description: GitHub organization or username
          default: devops-champ
          ui:autofocus: true

        repoName:
          title: Repository Name
          type: string
          description: Name of the GitHub repository (lowercase recommended)

        owner:
          title: Team Owner
          type: string
          ui:field: OwnerPicker
          ui:options:
            allowedKinds:
              - Group
    
  steps:
    - id: fetch-base
      name: Fetch Base
      action: fetch:template
      input:
        url: ./content
        copyWithoutTemplating:
        - .github/workflows/*
        values:
          repoOwner: ${{ parameters.repoOwner }}
          repoName: ${{ parameters.repoName | lower }}
          owner: ${{ parameters.owner }}

    - id: submit-request
      name: Submit Repository Request
      action: github:repository-request:create-pr
      input:
        repoOwner: ${{ parameters.repoOwner }}
        repoName: ${{ parameters.repoName | lower }}
        templateName: java25-template
        templateTitle: Java 25 Template
        repoVisibility: public
        defaultBranch: main
        catalogInfoPath: /catalog-info.yaml
        parameters:
          owner: ${{ parameters.owner }}

    - id: notify
      name: Notify
      action: notification:send
      input:
        recipients: entity
        entityRefs:
          - user:default/guest
        title: "Repository request submitted"
        info: "Java 25 repository request is pending Platform Team approval"
        severity: normal

  output:
    text:
      - title: Request ID
        content: ${{ steps['submit-request'].output.requestId }}
      - title: Status
        content: ${{ steps['submit-request'].output.status }}
    links:
      - title: Approval Pull Request
        url: ${{ steps['submit-request'].output.pullRequestUrl }}
      - title: Sonar Dashboard
        url: https://sonarcloud.io/project/overview?id=${{ parameters.repoName }}
      - title: Jenkins
        url: http://localhost:9090/job/devops-champ/job/${{ parameters.repoName }}/job/main
